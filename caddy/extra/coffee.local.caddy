# THE COFFEE STORE'S VITRINE, ON THE BENCH EDGE.
#
# `caddy/Caddyfile.local` ends with `import /etc/caddy/extra/*.local.caddy`, and this is the file that hole
# was left for. It adds a front the committed edge does not know about, and it edits nothing.
#
# ── WHY A PATH AND NOT A HOSTNAME ────────────────────────────────────────────────────────────────────────
# On this bench a store is reached at `/s/<handle>` (host -> store is DATA, and the bench has one origin).
# The coffee store's handle is `cafe`, so everything under `/s/cafe` is this shop and goes to its own
# container; every other store on the box keeps falling through to the reference vitrine. In a real
# deployment the same block is written against `{$FORGE_COFFEE_DOMAIN}` instead, and the container does not
# change: the front resolves its store from the request either way.
#
# ⚠️ SPECIFICITY, NOT ORDER. Caddy matches `handle` blocks by how SPECIFIC the path is, wherever they appear
# — so this wins over `Caddyfile.local`'s bare fall-through no matter which file is read first. It is written
# here because that is where the seam is, not because position decides anything.
#
# ⚠️ AND WHAT IS DELIBERATELY *NOT* ROUTED HERE — this is the part that fails silently if got wrong:
#
#   · `/checkout*`, `/account*`, `/_checkout/*`, `/apps/*` — the CHECKOUT's, and more specific than this
#     block, so they already win. Written down anyway because "why does the coffee checkout look like the
#     vanilla one" has exactly one right answer: it is supposed to. It wears the THEME, not the fork.
# ⚠️⚠️ TWO MATCHERS, AND THE REASON IS A DEFECT THIS BOX HAS TODAY — not a preference.
#
# A store's WORKING address here is `/s/<store id>`. The pretty `/s/<handle>` answers 200 with a whole page
# and NO STORE: every public read takes an id and refuses a handle (measured — `products`, `categories` and
# `store_flags` all 404 on `?store=outlet`), so the front degrades to its neutral title and an empty
# catalogue. That is true of all three shops on this box, it is not this slice's, and it has a card of its
# own (opened by C2, corroborated by D2 and reached a third time from here).
#
# So BOTH are pointed at this container, and each for its own reason:
#
#   · the ID is the address that WORKS, and it has to be here or `/s/<the coffee id>` would fall through to
#     the REFERENCE vitrine — the coffee shop rendered in the vanilla design, which is worse than a 404
#     because it looks like a decision;
#   · the HANDLE is the address humans type and the README used to promise. Pointed here it degrades exactly
#     the way the other two shops degrade, which is the honest behaviour: one defect, one shape, everywhere.
#     The day the handle card lands, this line starts working with no edit.
#
# ⚠️ THE ID IS THIS BOX'S AND CHANGES IF THE INSTANCE IS RE-PROVISIONED. `bash bin/seed.mjs` prints it, and
# `read.stores` on the internal face lists it. If the coffee shop ever comes up wearing the vanilla design,
# this line is the first thing to check — that is the symptom of a stale id, and it is silent.
# ⚠️⚠️⚠️ THE CHECKOUT'S PATHS UNDER THIS STORE ARE CEDED BACK, AND THIS BLOCK IS NOT OPTIONAL.
#
# The wildcard below does not match only the vitrine: `/s/<store>*` also covers `/s/<store>/checkout` and
# `/s/<store>/account`, which belong to the CHECKOUT container. `Caddyfile.local` already routes those with
# `handle /s/*/checkout*` — but this file's pattern carries a long literal store id, so it is the more
# SPECIFIC of the two and it wins. Measured: `/s/<cafe>/checkout` came back 404 through the edge while the
# checkout container answered 200 to the same path directly.
#
# ⚠️ DO NOT "SIMPLIFY" THIS BACK TO ONE WILDCARD. That is the whole warning: the shop's "Finalizar compra"
# then leads to this container's 404, and — as the header of `Caddyfile.local` puts it three lines above its
# own version of these routes — *a shop whose "Finalizar compra" leads to the vitrine's 404 is the same
# defect the front split exists to prevent, one layer out*. The two fronts share a hostname precisely so a
# shopper can cross between them carrying their cart cookie; a matcher that swallows the crossing undoes it.
#
# Both spellings of the store are listed for the same reason both are routed below.
# ⚠️ ONE BLOCK PER PATH, and not one block listing four: `handle` takes a SINGLE matcher token, and a list
# is a parse error — which does not degrade this store, it takes the WHOLE EDGE DOWN (measured: every host
# on the box answered nothing at all until this file parsed again). A named matcher would compile, but a
# `handle` with one has no path for Caddy to sort by, and sorting is exactly what makes these win over the
# wildcard below.
handle /s/sto_01M1DE555TJ36TQB6E9PR5VSJ4/checkout* {
	reverse_proxy checkout:3000
}

handle /s/sto_01M1DE555TJ36TQB6E9PR5VSJ4/account* {
	reverse_proxy checkout:3000
}

handle /s/cafe/checkout* {
	reverse_proxy checkout:3000
}

handle /s/cafe/account* {
	reverse_proxy checkout:3000
}

handle /s/sto_01M1DE555TJ36TQB6E9PR5VSJ4* {
	reverse_proxy storefront-coffee:3000
}

handle /s/cafe* {
	reverse_proxy storefront-coffee:3000
}

# ★★ THE ASSETS, AND THIS IS THE BLOCK THAT FAILS IN SILENCE IF IT IS WRONG.
#
# Splitting the vitrines by PATH does nothing for an asset URL, because an asset URL starts at the ROOT:
# every Next app on this origin asks the browser for `/_next/static/…` by default, and the edge can send
# that to only one container. The other one then loads chunks from a server that never emitted them — and
# nothing anywhere says so. The HTML arrives, no server logs a thing, and the shop is UNSTYLED. This front
# therefore sets `assetPrefix: '/_coffee'` (storefront-coffee/next.config.mjs) and this block is the half at
# the edge.
#
# ⚠️⚠️ `handle_path`, NOT `handle` — six characters and the whole thing. `handle_path` STRIPS the matched
# prefix before proxying. MEASURED on the real image in CHECKOUT-APP C3: a Next server with an `assetPrefix`
# EMITS the prefixed URL into its HTML and then answers **404** to it — it serves those files at
# `/_next/static/…` and nowhere else. The prefix is an instruction to THIS FILE, not a route the app owns.
# With a bare `handle` every chunk reaches the right container and comes back 404, which is the same
# unstyled shop the prefix exists to prevent, one layer further in.
handle_path /_coffee/* {
	reverse_proxy storefront-coffee:3000
}
