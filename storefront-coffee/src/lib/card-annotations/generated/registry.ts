// GENERATED by @forgecommerce/surface-codegen from composition.json — do not edit.
// It is DERIVED from this project's own composition, by a tool this project runs. Add or remove an app in
// composition.json and run `npm run codegen`; `npm test` fails while this file disagrees with
// that list, which is how you find out before a visitor does.
// The storefront.card_annotations half: the apps that annotate a product card, typed against the consumer's own contract.

import type { CardAnnotationsContribution } from '../contract';
import { fetchRatingSummaries as reviewsContribution } from '@forgecommerce/ext-reviews/ratings';

/** The apps contributing to `storefront.card_annotations`, in list order — 0..n, and ZERO is an ordinary state: an
 * instance whose list carries no contributor simply gets nothing here, and the consumer degrades. */
export const composedCardAnnotations: { id: string; contribute: CardAnnotationsContribution }[] = [
  { id: 'reviews', contribute: reviewsContribution },
];
