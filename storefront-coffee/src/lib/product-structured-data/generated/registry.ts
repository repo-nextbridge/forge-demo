// GENERATED by @forgecommerce/surface-codegen from composition.json — do not edit.
// It is DERIVED from this project's own composition, by a tool this project runs. Add or remove an app in
// composition.json and run `npm run codegen`; `npm test` fails while this file disagrees with
// that list, which is how you find out before a visitor does.
// The storefront.product_structured_data half: the apps that enrich a product's schema.org graph, typed against the consumer's own contract.

import type { ProductStructuredDataContribution } from '../contract';
import { productReviewMarkup as reviewsContribution } from '@forgecommerce/ext-reviews/seo';

/** The apps contributing to `storefront.product_structured_data`, in list order — 0..n, and ZERO is an ordinary state: an
 * instance whose list carries no contributor simply gets nothing here, and the consumer degrades. */
export const composedProductStructuredData: { id: string; contribute: ProductStructuredDataContribution }[] = [
  { id: 'reviews', contribute: reviewsContribution },
];
