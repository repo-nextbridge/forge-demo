// GENERATED by @forge/codegen from extensions/composition.base.json — do not edit.
// The URLs the composed apps serve from a root-level route handler — what the middleware must NOT rewrite.
// Adding an app here by hand is a red build (codegen drift); importing a listed app outside a generated
// file is a red build (scripts/composition/composition-imports.guard.test.ts). Take an app OUT by removing
// its entry from the list and running `pnpm codegen` — nothing else knows its name.

/** Every public URL an app serves through THIS front, in path order — 0..n, and ZERO is an ordinary
 * state, and the ordinary one for a front no app's route resolves to: the composition decides where each
 * route lands (see `frontConsumerOfRoute`), so an empty list here means the other front hosts them. */
export const APP_PUBLIC_ROUTE_PATHS: readonly string[] = ['/feeds/google.xml'];
