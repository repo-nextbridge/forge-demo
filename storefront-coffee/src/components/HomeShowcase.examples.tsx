// Gallery example (/ui-storefront/home-showcase) — the home body rendered with fixtures so its fidelity is
// reviewable and its live behaviours (hero dots/autoplay, newsletter `.om-invalid` + shake) are drivable offline.
// The data-driven pieces (shelves, banner mosaic, vitrine) need the read port and are verified on Staging.

import { HomeShowcase } from './HomeShowcase';

export function HomeShowcaseExamples() {
  return <HomeShowcase />;
}
